name: code_builder
description: A new brick created with the Mason CLI.
version: 0.1.0+1

environment:
  mason: ^0.1.1

vars:
  builder_name:
    type: string
    description: Name of the builder
    prompt: What is the name of the builder?
